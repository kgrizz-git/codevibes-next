# .gitleaks.toml
# No allowlist: verified with gitleaks v8.30.1 that no tracked file, git
# history entry, or .env.example requires an exception. Keep it that way —
# codevibes-backend/data/ is gitignored and untracked; never re-add broad
# directory exclusions here.
