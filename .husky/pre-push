#!/usr/bin/env sh
set -e

# Pre-push: typecheck + AFFECTED tests (frontend + backend), with a safe
# fallback to the full suites when an affected-test merge base cannot be
# established (e.g. first push with no upstream, or an unborn history).
npm run typecheck

node scripts/test-affected.mjs
