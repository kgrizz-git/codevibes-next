# To Do

## Agent harness and maintenance

- **Implement the agent-harness plan** in `plans/harness-engineering.md`: establish the root command contract, a compact `repo:map`, scoped guidance only where warranted, and CI-visible guidance/document checks. Keep accepted long-lived choices in `plans/decisions/`.
- **Make developer documentation truthful and navigable.** Align README setup and architecture guidance with `.nvmrc`, executable npm scripts, the fork workflow, and current backend modules; replace positional “TO_DO item” references with named headings or direct links.
- **Retire structural legacy ceilings.** `check:structure` and `check:complexity` already block regressions. Split `sidebar.tsx`, `HomePage.tsx`, and `AnalyzePage.tsx` below the standard limits, lower/remove their checked-in ceilings as work lands, and remove the `deepseekService.ts` ceiling only with legacy-provider retirement.
- **Increase targeted test coverage at high-risk boundaries.** Add tests for API/auth/CSRF failure modes, analysis/SSE contracts, file selection and token-cost calculations, and frontend analysis state transitions. Establish non-zero per-directory coverage floors only after the relevant tests exist, then ratchet them for changed code.
- **Standardize upkeep checks.** Markdown-link/guidance and review-pipeline documentation checks are now deterministic and CI-visible; baseline the bundle-size budget, and schedule non-blocking dependency, Browserslist, and plan/decision freshness reviews.

## Product roadmap

- **(DONE)** explore and document how codevibes works end-to-end: the written reference lives at `docs/review-pipeline/` (index: `docs/review-pipeline.md`), covering file selection (`fileFilter.ts`), discovery (`githubService.ts`), orchestration/SSE (`analysisService.ts`), the reviewing agent (`deepseekService.ts`), and the cost model (`tokenCounter.ts`). Future work (provider support, prompt tuning) should treat these as the source of truth and update them when the pipeline changes (see AGENTS.md).
- expand support for other model providers — draft plan in `plans/model-provider-compatibility.md` (NEEDS REVIEW). Near-term scope: OpenAI-compatible APIs only (DeepSeek already speaks this); track broader expansion (Anthropic Messages, Google Gemini, AWS Bedrock, Cohere, xAI, ...) as follow-up work behind an adapter abstraction.
- **Broader language & pattern coverage — do this immediately after item 1.** The end-to-end documentation from item 1 must land first so the file-selection and prompt-building rules are fully understood before they are extended. Scope:
  - expand file categorization, ignore patterns, and review pattern matching for more languages and greater variation (currently scoped to a limited set).
  - **Explore enhancing reviews further**, not just breadth: introduce **selectable layers of effort and detail** for the reviewing agents (e.g. a lightweight "quick pass" vs. a deep "thorough pass"), with the agent prompt, file cap, parallelism, and token budget scaling to the chosen layer. Investigate how effort/detail layers should be surfaced in the UI, persisted per-analysis, and reported in the `complete`/estimate payloads. Capture the design (layers, defaults, token/scope trade-offs) in `plans/` alongside the item-1 reference doc.
  - **Keep the pipeline docs in sync:** any change here (new language extensions, new P1/P2 patterns, effort layers) must also update `docs/review-pipeline/` — specifically `02-file-selection.md` (language/pattern lists) and `06-extension-hooks.md` (item 5 hooks), and `04-reviewing-agent.md` if prompt depth changes. Treat those docs as the spec for this work.
  - **Draft plans exist (NEEDS REVIEW):** `plans/item5-overview.md` splits item 5 into three linked plans — `plans/item5-a-broader-languages.md` (languages/patterns, low risk, can ship first), `plans/item5-b-effort-layers.md` (effort/detail layers, cross-cutting, phased B0–B3), and `plans/item5-c-docs-sync.md` (keep `docs/review-pipeline/` accurate). Review/approve before implementation.
- retire quality-gate legacy ceilings: reduce structural ESLint diagnostics and split oversized `sidebar.tsx`, `HomePage.tsx`, and `AnalyzePage.tsx`; the blocking baselines must never be raised. **Exception:** `deepseekService.ts` must remain byte-for-byte intact until `USE_LEGACY_PROVIDER` is retired (see plan Step 1 legacy preservation); its explicit ceiling prevents growth until then.
